backend:
  name: github
  repo: munizcesar/InSpotGO-US
  branch: main
  base_url: https://inspotgo.com
  auth_endpoint: api/auth
  app_id: Ov23liut527fMI71NRQR

media_folder: "public/images/posts"
public_folder: "/images/posts"

locale: en

collections:
  - name: posts
    label: Posts
    folder: src/content/posts
    create: true
    slug: "{{year}}-{{slug}}"
    fields:
      - {label: Title, name: title, widget: string}
      - {label: Date, name: date, widget: datetime}
      - {label: Category, name: category, widget: select, options: ["Tech", "SaaS", "Software", "Guides"]}
      - {label: Tags, name: tags, widget: list, required: false}
      - {label: Description, name: description, widget: text}
      - {label: Cover Image, name: cover, widget: image, required: false, media_folder: "/public/images/posts", public_folder: "/images/posts"}
      - {label: Read Time, name: readTime, widget: string, required: false, hint: "Ex: 5 min"}
      - {label: Featured, name: featured, widget: boolean, required: false, default: false}
      - {label: Draft, name: draft, widget: boolean, required: false, default: false}
      - label: Body
        name: body
        widget: markdown
        media_folder: "/public/images/posts"
        public_folder: "/images/posts"
        buttons:
          - bold
          - italic
          - code
          - link
          - heading-one
          - heading-two
          - heading-three
          - bulleted-list
          - numbered-list
          - quote
        editor_components:
          - image
          - code-block
          - callout
          - cta-button
          - pros-cons
          - youtube
        modes:
          - rich_text
          - raw
